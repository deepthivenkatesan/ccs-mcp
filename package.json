{
  "name": "ccs-mcp",
  "version": "1.0.0",
  "description": "CCS MCP Gateway: one Cloudflare Worker hosting many MCP connectors, one per URL path.",
  "main": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy:stage": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1"
  }
}
